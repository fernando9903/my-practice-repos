/* Data Types:
    undefined, null, boolean, string, symbol, bumber, and object
*/

//this var can be access in the hole project
var myName = 'Fernando';

myName =  8;

//This var only can be access into the scope where was declare
let computersName = 'Dell';

//I valu that never change
const pi = Math.PI;